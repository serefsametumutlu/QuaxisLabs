# Bilanco Radar - Python bagimliliklari

# HTTP ve dayaniklilik
httpx>=0.27
tenacity>=8.2

# KAP finansal rapor HTML ayristirma (bkz. src/fetchers/kap_financials.py)
beautifulsoup4>=4.12

# KAP fon "Portfoy Dagilim Raporu" PDF ayristirma (bkz. src/fetchers/kap_fund_portfolio.py)
pdfplumber>=0.11

# Taranmis/goruntu-tabanli PDF'ler icin OCR yedegi (bkz. src/fetchers/pdf_ocr.py) --
# Tesseract OCR motorunun kendisi (binary) ve Turkce dil paketi (tur.traineddata)
# AYRICA kurulmalidir, pip bunlari saglamaz -- bkz. PROJE_HAFIZASI/07_BAKIM_KURALLARI.md
PyMuPDF>=1.24
pytesseract>=0.3.13

# Veritabani
SQLAlchemy>=2.0

# Gorsel uretim
Jinja2>=3.1
playwright>=1.45
Pillow>=10.0

# Telegram bot
python-telegram-bot>=21.0

# Konfigurasyon
python-dotenv>=1.0

# Test
pytest>=8.0
pytest-asyncio>=0.24

# ABCD harmonik formasyon modulu icin (bkz. src/fetchers/abcd_data.py,
# docs/spec/spec_abcd_mimari_kararlar.md Karar 3/4) -- sadece bu modulde
# kullanilir, projenin geri kalanini etkilemez.
pandas>=2.0
numpy>=1.26
pyarrow>=15
yfinance>=0.2

# ABCD basari faktoru analizi icin (bkz. src/analysis/abcd_factor_analysis.py) --
# Mann-Whitney/ki-kare/Fisher-exact/Benjamini-Hochberg FDR icin scipy.stats;
# yorumlanabilir lojistik regresyon icin statsmodels.Logit (katsayi + Wald p +
# %95 CI DOGRUDAN saglar -- sklearn'in LogisticRegression'i bunlari vermez,
# manuel Hessian-tabanli Wald hesabi yazmak gereksiz/riskli olurdu). Sadece bu
# modulde kullanilir.
scipy>=1.11
statsmodels>=0.14

# Wavelet Trend Rider stratejisi icin (bkz. src/analysis/wavelet_trend_rider.py) --
# Daubechies-8 dalgacik gurultu temizlemesi, sadece bu modulde kullanilir.
PyWavelets>=1.5

# Yeni strateji arastirmasi -- ornek sinyal mumlarini gorsellestirmek icin
# (bkz. scripts/strateji_arastirma_backtest.py), sadece scripts/ katmaninda.
matplotlib>=3.8
mplfinance>=0.12
